/* Add, remove, or reorder apps by editing this list, then refresh the page
   (no server, no permission prompt — it's just a script tag).

   Order here is the display order. There's no live folder-scanning, so
   `folder` has to match the real folder name under apps/ exactly, including
   capitalization.

   `desc` and `readme` are optional. Leaving out `desc` repeats the folder
   name as the subheader. Leaving out `readme` shows "No readme provided"
   in the About panel. The app's logo (apps/<folder>/app-logo.png) isn't
   listed here — just drop the file in the folder and it's picked up
   automatically; no logo falls back to a letter badge.
*/

window.ZINENOW_APPS = [  
  {
    folder: "ZineArranger",
    desc: "Prepare Zine for Printing"
  },
  {
    folder: "Markdown-to-PDF",
    desc: "Md text editor for PDF Rxport",
  }
];
